// tailwind.config.js
module.exports = {
    darkMode: 'class', // Enable class-based dark mode
    content: [
      './index.html', // Add paths to your HTML files
      './script.js',  // Add paths to your JS files (if they contain Tailwind classes)
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  };